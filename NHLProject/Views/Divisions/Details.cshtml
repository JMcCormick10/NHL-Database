@model NHLProject.Models.DivisionTeam

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Division</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.divisions.Name);
        </dt>

        <dd>
            @Html.DisplayFor(model => model.divisions.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.divisions.Conference.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.divisions.Conference.Name)
        </dd>

    </dl>
</div>
<div>
    <ul>
        @foreach (var team in Model.teams)
        {
            <li>@team.City @team.Name</li>
        }
    </ul>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.divisions.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
