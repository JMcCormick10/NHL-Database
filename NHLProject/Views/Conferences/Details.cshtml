@model NHLProject.Models.ConferenceDivision

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Conference</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.conferences.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.conferences.Name)
        </dd>

    </dl>
</div>
<div>
    <ul>@foreach (var division in Model.divisions)
    {
        <li>@division.Name</li>
    }

    </ul>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.conferences.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
